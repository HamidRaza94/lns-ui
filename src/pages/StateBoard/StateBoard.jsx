import React from 'react';
import { withStyles } from '@material-ui/core';

import styles from './style';

const StateBoard = () => {
  return (
    <div>
      <h1>StateBoard Page</h1>
    </div>
  );
};

export default withStyles(styles)(StateBoard);
